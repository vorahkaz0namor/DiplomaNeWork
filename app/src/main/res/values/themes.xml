<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.DiplomaNeWork" parent="Theme.MaterialComponents.DayNight.DarkActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/purple_500</item>
        <item name="colorPrimaryVariant">@color/purple_700</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
        <item name="actionBarStyle">@style/Theme.DiplomaNeWork.AppBar</item>
        <item name="actionBarTheme">@style/Theme.DiplomaNeWork.CustomActionBar</item>
    </style>

    <style name="Theme.DiplomaNeWork.AppBar" parent="@style/Widget.MaterialComponents.Light.ActionBar.Solid">
        <item name="background">@color/ripple_button</item>
    </style>

    <style name="Theme.DiplomaNeWork.CustomActionBar" parent="@style/ThemeOverlay.MaterialComponents.Dark.ActionBar">
        <item name="iconTint">?attr/colorControlNormal</item>
    </style>

</resources>